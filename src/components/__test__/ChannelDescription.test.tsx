/* eslint-disable testing-library/no-unnecessary-act */
import React from 'react';
import { act, render, screen } from '@testing-library/react';
import ChannelDescription from '../ChannelDescription';

test('testChannelDescription', async () => {
    /*
    await act(() => {
        render(<ChannelDescription channel= {{id: "channelId1", name: "TestChannel 1", description: "Erster Channel",
         owner: "Hans", ownerId: "HansId", createdAt: "20.12.2022", messageCount: 0, public: true, closed: false}}></ChannelDescription>);
    });
    const linkElement = screen.getByText(/TestChannel 1/i);
    expect(linkElement).toBeInTheDocument(); 
    */
});
